# Event Core - Tenant Frontend Production Environment
# Copy this file to .env.prod on server

# API Base URL - points to api.eventcoresolutions.com (backend gateway)
NEXT_PUBLIC_API_BASE_URL=https://api.eventcoresolutions.com
# Root domain for multi-tenant subdomains
NEXT_PUBLIC_ROOT_DOMAIN=eventcoresolutions.com
# Environment mode
NEXT_PUBLIC_APP_ENV=production

# Note: Admin tenant ID (SAAS_TENANT_ID) is now fetched dynamically from backend API
# by calling: GET /tenants/public/resolve?subdomain=admin
# No need to specify it in this file anymore

